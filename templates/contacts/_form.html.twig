{{ form_start(form) }}

<div class="card card-info">
    <div class="card-header">
        <h3 class="card-title">Reclamation</h3>
    </div>
    <div class="card-body">

        <div class="form-group">
            <label class="col-sm-2 col-form-label">Contenu</label>
            {{ form_widget(form.Body,{'attr':{'class': 'form-control','placeholder':'ecrire reclamation'}}) }}
        </div>
        <div class="form-group">
            <label class="col-sm-2 col-form-label">hotels</label>
            {{ form_widget( form.ID_hotel ,{'attr':{'class': 'form-control','placeholder':'ecrire reclamation'}}) }}
        </div>


        <button class="btn btn-success pull-right">{{ button_label|default('Save') }}</button>

    </div>
</div>

{{ form_end(form) }}