<form method="post" action="{{ path('app_category_event_delete', {'id': category_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_event.id) }}">
    <button class="btn">Delete</button>
</form>
