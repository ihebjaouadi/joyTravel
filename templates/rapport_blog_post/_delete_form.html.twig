<form method="post" action="{{ path('app_rapport_blog_post_delete', {'id': rapport_blog_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_blog_post.id) }}">
    <button class="btn">Delete</button>
</form>
