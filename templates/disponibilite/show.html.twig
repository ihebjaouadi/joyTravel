{#{% extends 'base.html.twig' %}#}
{% extends 'baseBackOffice.html.twig' %}
{% block title %}Disponibilite{% endblock %}

{% block body %}
    <h1>Disponibilite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disponibilite.id }}</td>
            </tr>
            <tr>
                <th>Reservee_du</th>
                <td>{{ disponibilite.reserveeDu ? disponibilite.reserveeDu|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Reservee_au</th>
                <td>{{ disponibilite.reserveeAu ? disponibilite.reserveeAu|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_disponibilite_index') }}">back to list</a>

    <a href="{{ path('app_disponibilite_edit', {'id': disponibilite.id}) }}">edit</a>

    {{ include('disponibilite/_delete_form.html.twig') }}
{% endblock %}
