{% extends 'baseBackOffice.html.twig' %}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
    <h1>Edit Hotel</h1>

    {{ include('hotel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_hotel_index') }}">
        <button class="btn btn-success ">{{ button_label|default('Back to list') }}</button>
    </a>

    {{ include('hotel/_delete_form.html.twig') }}
{% endblock %}
