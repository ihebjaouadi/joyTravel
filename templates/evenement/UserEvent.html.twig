
{% extends 'base.html.twig' %}
{% block body %}


<!DOCTYPE html>
<html lang="en">
<head>
    <title>Bootstrap 4 Website Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .fakeimg {
            width: 100%;
            height: auto;
            vertical-align: middle;

        }
    </style>
</head>
<body>


<div class="container" style="margin-top:30px">
    <div class="row">
        <div class="col-sm-4">
            <h2>About Me</h2>
            <h5>Photo of me:</h5>
            <div class="fakeimg"><form method="post" action="{{ path('rechercheUser') }}">
                    <label  >Rechercher</label>
                    <input type="text" name="searchUser"><input type="submit" value="Recherche">
                </form></div>

<br><br>

            <p>----------------------------------------</p>
            <h3>price <i class="fa fa-money" aria-hidden="true"></i>
            </h3>
            <a href="{{ path('TrieUser') }}"> <button type="button" class="btn-success" > Trie ASC  </button></a> <br>

            <a href="{{ path('TriPriceDESCUser') }}"><button type="button" class="btn-success" > Trie DESC  </button></a>
            <p>----------------------------------------</p>
            <p>Lorem ipsum dolor sit ame.</p>
            <ul class="nav nav-pills flex-column">
                <li class="nav-item">
                    <a class="nav-link active" href="#">Active</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Link</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Link</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href="#">Disabled</a>
                </li>
            </ul>
            <hr class="d-sm-none">
        </div>
        <div class="col-sm-8">
            {% for evenement in evenements %}

            <h2 class="text-bold">{{ evenement.Nom }}</h2>
            <h5> {{ evenement.Prix }}</h5>
            <a href="{{ path('user_show', {'id': evenement.id}) }}" class="view" title="View" data-toggle="tooltip">
            <div class="fakeimg"><img src="{{ asset('uploads/images/'~ evenement.Img) }}" style="width: 400px; height: 400px;"></div>
            </a>

            <p>{{ evenement.Description }}</p>
            <br>

                <h5><i class='fas fa-hourglass-end'  > {{  evenement.DateDebut ? evenement.DateDebut|date('Y-m-d') : ''  }} </i></h5> to
                <h5><i class='fas fa-hourglass-end'  > {{  evenement.DateFin ? evenement.DateFin|date('Y-m-d') : ''  }} </i>
                </h5>

       {% endfor %}

        </div>
    </div>
</div>

<div class="jumbotron text-center" style="margin-bottom:0">
    <p>Footer</p>
</div>

</body>
</html>
{#


{% extends 'base.html.twig' %}


{% block body %}










{% for evenement in evenements %}
<div class="w3-quarter">
	<a href="{{ path('app_evenement_show', {'id': evenement.id}) }}">
		<img src="{{ asset('uploads/images/'~ evenement.Img) }}" style="width:20%"  />
	</a>
	<h3>	{{ evenement.Nom }}</h4>
	<h2>	{{ evenement.Prix }}</h4>
	{% endfor %}

{% endblock %}








#}

{% endblock %}

{% block footer %}
{% endblock %}